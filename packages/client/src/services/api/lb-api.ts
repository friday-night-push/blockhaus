// import { getResponseOrThrow } from 'src/utils/get-response-or-throw';

import BaseAPI from './base-api';

export default class LeaderboardAPI extends BaseAPI {
  // getAll(data: TUserProfileRequest, cb: (u: TUser) => void, errorCb: TErrorFn): Promise<unknown> {
  //   return this.post<TUserProfileRequest>('/user/fnp_team', data})
  //     .then(async response => await (await getResponseOrThrow(response)).json())
  //     .then(cb)
  //     .catch(errorCb);
  // }
}
